{% for product in collections['Hydrogen'].products %}
  <div class="product">
    <h2>{{ product.title }}</h2>
    <p>Precio: {{ product.price }}</p>
    <img src="{{ product.featured_image | img_url: width: 200 }}" alt="{{ product.title }}" />
  </div>
{% endfor %}