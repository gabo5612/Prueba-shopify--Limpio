{% for product in collections['Hydrogen'].products %}
  <div class="product">
    <h2>{{ product.title }}</h2>
    <p>Precio: {{ product.price }}</p>
    <img src="{{ product.images.src[0] | img_url: width: 200 }}" alt="{{ product.title }}" />
  </div>
{% endfor %}